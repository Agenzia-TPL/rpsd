apiVersion: v1
kind: Service
metadata:
  name: kafka-ui
spec:
  selector:
    app.kubernetes.io/name: kafka-ui
    app.kubernetes.io/part-of: rpsd
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  type: ClusterIP
