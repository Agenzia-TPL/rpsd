apiVersion: v1
kind: Service
metadata:
  name: keycloak-db
spec:
  selector:
    app.kubernetes.io/name: keycloak-db
    app.kubernetes.io/part-of: rpsd
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
  type: ClusterIP
