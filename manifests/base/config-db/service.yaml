apiVersion: v1
kind: Service
metadata:
  name: config-db
spec:
  selector:
    app.kubernetes.io/name: config-db
    app.kubernetes.io/part-of: rpsd
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
  type: ClusterIP
