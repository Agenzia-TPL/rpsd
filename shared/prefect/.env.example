# Prefect shared service overrides (optional)
# Most settings are in compose/shared-prefect.yml with sensible defaults.

# RPSD_PREFECT_PORT=19200
# RPSD_PREFECT_DB_USER=prefect
# RPSD_PREFECT_DB_PASS=prefect
# RPSD_PREFECT_DB_NAME=prefect
